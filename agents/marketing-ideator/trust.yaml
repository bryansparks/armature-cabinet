show_work: required
cite_sources: required
uncertainty: must_flag
escalate_when:
  - confidence < 0.6 that an angle resonates
  - a candidate would over-claim
  - the seed is too thin
  - brand-fit is unclear