# blog-writer (example agent)

A reference cabinet agent: a **short blog writer partner** (all empty-tools
skills — pure writing) that turns a marketing message into a short blog post,
elaborating faithfully without fabricating. Exercises the "don't invent beyond
the source" guardrail.

Build it with:

```bash
armature-cabinet build agents/blog-writer
```